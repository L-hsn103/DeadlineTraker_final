<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.deadline_tracker">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO"/>
    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"/>

    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:mimeType="application/pdf"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:mimeType="image/*"/>
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DeadLineTracker">

        <!-- Cloudinary configuration -->
        <meta-data
            android:name="CLOUDINARY_URL"
            android:value="cloudinary://338178119728697:y9dbkZMcZz4BHtpqog4cEvqrz9Y@dypmkxzcw"/>

        <!-- Entry point -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.DeadLineTracker">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Auth — keep default transition -->
        <activity android:name=".LoginActivity" android:exported="false"/>
        <activity android:name=".RegisterRoleActivity" android:exported="false"/>
        <activity android:name=".RegisterStudentActivity" android:exported="false"/>
        <activity android:name=".RegisterTeacherActivity" android:exported="false"/>

        <!-- ✅ Student — instant switch, no slide animation -->
        <activity android:name=".StudentDashboardActivity"
            android:exported="false"
            android:theme="@style/Theme.NoAnimation"/>
        <activity android:name=".NotificationListActivity"
            android:exported="false"
            android:theme="@style/Theme.NoAnimation"/>
        <activity android:name=".SettingsActivity"
            android:exported="false"
            android:theme="@style/Theme.NoAnimation"/>

        <!-- ✅ Teacher — instant switch, no slide animation -->
        <activity android:name=".TeacherDashboardActivity"
            android:exported="false"
            android:theme="@style/Theme.NoAnimation"/>
        <activity android:name=".AddTaskActivity"
            android:exported="false"
            android:theme="@style/Theme.NoAnimation"/>
        <activity android:name=".TeacherNotificationActivity"
            android:exported="false"
            android:theme="@style/Theme.NoAnimation"/>
        <activity android:name=".TeacherSettingsActivity"
            android:exported="false"
            android:theme="@style/Theme.NoAnimation"/>
        <activity android:name=".SubmissionsActivity"
            android:exported="false"
            android:theme="@style/Theme.NoAnimation"/>

        <!-- ✅ Shared — instant switch, no slide animation -->
        <activity android:name=".CalendarActivity"
            android:exported="false"
            android:theme="@style/Theme.NoAnimation"/>

        <!-- TaskDetail keeps normal transition (feels right opening a task) -->
        <activity android:name=".TaskDetailActivity"
            android:exported="false"/>

        <!-- File provider -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"/>
        </provider>

    </application>
</manifest>